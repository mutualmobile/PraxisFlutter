name: praxis_flutter_domain
description: A new Flutter project.
version: 0.0.1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  clean_architecture:
    path: ../clean_architecture
